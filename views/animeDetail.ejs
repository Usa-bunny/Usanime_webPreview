<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= anime.title %> - Detail Anime</title>

    <!-- BOOTSTRAP -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <link rel="stylesheet" href="/css/style.css">

    <style>
        /* KONTEN BERGESER KETIKA DESKTOP */
        @media (min-width: 768px) {
            .content-with-sidebar {
                margin-left: 260px; /* menyesuaikan lebar sidebar */
            }
        }

        body {
            background-color: #f8f9fa;
            color: #333;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        /* HEADER DETAIL */
        .detail-header {
            background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), url('<%= anime.cover %>');
            background-size: cover;
            background-position: center;
            color: white;
            padding: 80px 0 40px;
            margin-bottom: 30px;
        }

        .anime-cover-large {
            width: 100%;
            max-width: 300px;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .rating-badge-large {
            background: rgba(0,0,0,.7);
            color: #ffd700;
            padding: 8px 15px;
            border-radius: 10px;
            font-size: 1rem;
            font-weight: 600;
            display: inline-block;
            margin-top: 15px;
        }

        /* KONTEN DETAIL */
        .detail-content {
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            padding: 25px;
            margin-bottom: 30px;
        }

        .genre-badge {
            background-color: #e9ecef;
            color: #495057;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            margin-right: 8px;
            margin-bottom: 8px;
            display: inline-block;
        }

        /* EPISODE LIST */
        .episode-card {
            border-radius: 10px;
            overflow: hidden;
            background: #fff;
            transition: .25s;
            border: 1px solid #ddd;
            box-shadow: 0 4px 10px rgba(0,0,0,0.08);
            text-decoration: none;
            color: inherit;
            display: block;
            height: 100%;
        }

        .episode-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0,0,0,0.15);
            text-decoration: none;
            color: inherit;
        }

        .episode-img {
            width: 100%;
            height: 140px;
            object-fit: cover;
        }

        .episode-info {
            padding: 15px;
        }

        .episode-title {
            font-weight: 600;
            font-size: 1rem;
            margin-bottom: 5px;
        }

        .episode-meta {
            color: #6c757d;
            font-size: 0.85rem;
        }

        /* BUTTONS */
        .btn-watch {
            background-color: #dc3545;
            border-color: #dc3545;
            color: white;
            font-weight: 600;
            padding: 10px 20px;
            border-radius: 8px;
        }

        .btn-watch:hover {
            background-color: #c82333;
            border-color: #bd2130;
            color: white;
        }

        .btn-wishlist {
            background-color: #6c757d;
            border-color: #6c757d;
            color: white;
            font-weight: 600;
            padding: 10px 20px;
            border-radius: 8px;
        }

        .btn-wishlist:hover {
            background-color: #5a6268;
            border-color: #545b62;
            color: white;
        }

        .btn-back {
            background-color: transparent;
            border: 1px solid #6c757d;
            color: #6c757d;
            font-weight: 600;
            padding: 10px 20px;
            border-radius: 8px;
        }

        .btn-back:hover {
            background-color: #6c757d;
            color: white;
        }

        /* SECTION TITLE */
        .section-title {
            font-weight: 700;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e9ecef;
        }
    </style>
</head>

<body class="anime-body">

    <!-- SIDEBAR DI SINI -->
    <%- include('sidebar') %>

    <!-- WRAPPER KONTEN -->
    <div class="content-with-sidebar">

        <!-- HEADER DETAIL -->
        <div class="detail-header">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col-md-4 text-center mb-4 mb-md-0">
                        <img src="<%= anime.cover %>" class="anime-cover-large" alt="<%= anime.title %>">
                        <div class="rating-badge-large">⭐ <%= anime.rating %>/10</div>
                    </div>
                    <div class="col-md-8">
                        <h1 class="display-5 fw-bold mb-3"><%= anime.title %></h1>
                        
                        <div class="mb-4">
                            <h5 class="d-inline me-3"><strong>Episode:</strong> <%= anime.episodes %></h5>
                            <h5 class="d-inline"><strong>Rating:</strong> <%= anime.rating %>/10</h5>
                        </div>
                        
                        <div class="mb-4">
                            <h5 class="mb-2"><strong>Genre:</strong></h5>
                            <div>
                                <% anime.genre.forEach(function(g) { %>
                                    <span class="genre-badge"><%= g %></span>
                                <% }); %>
                            </div>
                        </div>
                        
                        <div class="d-flex flex-wrap gap-3 mt-4">
                            <a href="<%= anime.link %>" target="_blank" class="btn btn-watch">
                                ▶ Tonton Sekarang
                            </a>
                            <a href="/usanime/wishlist/<%= anime._id %>" class="btn btn-wishlist">
                                ❤️ Tambah ke Wishlist
                            </a>
                            <a href="/usanime" class="btn btn-back">
                                ⬅ Kembali ke Daftar
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="container">

            <!-- SINOSIS -->
            <div class="detail-content">
                <h3 class="section-title">Sinopsis</h3>
                <p class="lead"><%= anime.synopsis %></p>
            </div>

            <!-- EPISODE LIST
            <div class="detail-content">
                <h3 class="section-title">Daftar Episode</h3>
                <div class="row g-4">
                    <% for(let i = 1; i <= Math.min(anime.episodes, 12); i++) { %>
                        <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                            <a href="<%= anime.link %>?episode=<%= i %>" target="_blank" class="episode-card">
                                <div>
                                    <img src="<%= anime.cover %>" class="episode-img" alt="Episode <%= i %>">
                                </div>
                                <div class="episode-info">
                                    <div class="episode-title">Episode <%= i %></div>
                                    <div class="episode-meta">
                                        <span>24 menit</span> • <span>HD</span>
                                    </div>
                                </div>
                            </a>
                        </div>
                    <% } %>
                </div>
                
                <% if (anime.episodes > 12) { %>
                    <div class="text-center mt-4">
                        <button class="btn btn-outline-secondary">Muat Lebih Banyak Episode</button>
                    </div>
                <% } %>
            </div> -->

        </div>

    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>