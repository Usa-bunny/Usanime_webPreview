<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kelola Anime</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="anime-body">
  <%- include('sidebar') %>

  <div class="content-wrapper">
    <div class="page-header">
      <h1 class="page-title">Kelola Anime</h1>
      <p class="page-subtitle">Kelola koleksi anime Anda</p>
    </div>

    <div class="anime-container">
        <% animeList.forEach(anime => { %>
            <div class="anime-item">
                <img src="/uploads/<%= anime.cover %>" alt="<%= anime.title %>" class="anime-cover">

                <div class="anime-info">
                    <h3><%= anime.title %></h3>
                    <p><strong>⭐ Rating:</strong> <%= anime.rating %>/10</p>
                    <p><strong>🏷️ Genre:</strong> <%= anime.genre.join(", ") %></p>
                    <p><strong>🎬 Episode:</strong> <%= anime.episodes %></p>
                    <p class="anime-synopsis"><strong>📖 Sinopsis:</strong> <%= anime.synopsis %></p>
                </div>

                <div class="anime-actions">
                    <a href="/anime/edit/<%= anime._id %>" class="btn btn-primary">✏️ Update</a>
                    <a href="/anime/delete/<%= anime._id %>" class="btn btn-danger" 
                       onclick="return confirm('Yakin ingin menghapus anime <%= anime.title %>?')">🗑️ Delete</a>
                </div>
            </div>
        <% }) %>
    </div>
  </div>
</body>
</html>